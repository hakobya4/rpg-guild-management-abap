projection;
strict ( 2 );
use draft;

define behavior for ZC_RPG_ADVENTURER alias Adventurer
{
  use create;
  use update;
  use delete;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;
  use action joinGuild;
  use action acceptQuest;
}